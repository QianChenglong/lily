#pragma once

NAME_BGN
/**
 * 打印windows错误信息
 */
void printError(char *fmt, ...);

/**
 * 返回windows错误信息字符串
 */
char* strerror();
/**
 * 获取指定适配器网关
 * @param [in] apdapter_uuid 适配器UUID
 * @param [out] gateway 网关
 * @return 操作结果
 */
NAME_END
